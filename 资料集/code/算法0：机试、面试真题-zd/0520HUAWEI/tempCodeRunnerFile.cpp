cin >> sname;
    // vector<Node> vec;
    // string line;
    // while(cin >> line){
    //     cout <<  string(line.begin(), line.begin() + line.find(',')) << " " << 
    //     string(line.begin() + line.find(',') + 1, line.end());
    // }