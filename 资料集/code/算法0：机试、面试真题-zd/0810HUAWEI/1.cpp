#include<bits/stdc++.h>
using namespace std;

vector<vector<int>> addBordar(vector<vector<int>> board)
{
    vector<vector<int>> res(board.size()+2, vector<int>(board[0].size()+2, 0));
    for(int i = 0; i < )
}

void game(vector<vector<int>> board)
{
    // 校验输入，如果只有一行或者一列，全部置0
    vector<vector<int>> nb = addBordar(board);
}