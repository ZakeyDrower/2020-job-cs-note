#include"header.h"

vector<pari<int,int>> process(const string & s)
{
    
}

TreeNode* recoverFromPreorder(string s)
{
    if(s == "") return nullptr;

    vector<pair<int, int>> po = process(s);
}

int main()
{
    string s = "";
    TreeNode * root = recoverFromPreorder(s);
}